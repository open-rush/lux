# @rush/integrations

外部服务集成客户端。

## 规划子模块

```
src/
├── git/                     # Git 操作（GitHub API, clone, push, PR 创建）
├── storage/                 # S3 兼容存储（MinIO / AWS，workspace snapshot, artifact 上传）
└── index.ts
```

## 依赖

无内部依赖。
